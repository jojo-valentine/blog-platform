info:
  name: logout
  type: http
  seq: 5

http:
  method: POST
  url: "{{base_url}}/api/auth/logout"
  body:
    type: json
    data: |-
      {
        
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

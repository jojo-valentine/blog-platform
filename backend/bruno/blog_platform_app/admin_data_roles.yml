info:
  name: admin_data_roles
  type: http
  seq: 30

http:
  method: GET
  url: "{{base_url}}/api/admin/roles?search={{search}}&page={{page}}"
  headers:
    - name: Cookie
      value: accessToken={{jwt_token}}
  params:
    - name: search
      value: "{{search}}"
      type: query
    - name: page
      value: "{{page}}"
      type: query
  auth: inherit

runtime:
  variables:
    - name: search
      value: ""
    - name: page
      value: "1"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

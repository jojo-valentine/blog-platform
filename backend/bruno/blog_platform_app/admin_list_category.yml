info:
  name: admin_list_category
  type: http
  seq: 25

http:
  method: GET
  url: "{{base_url}}/api/admin/list/categories?search={{search}}"
  headers:
    - name: Cookie
      value: accessToken={{jwt_token}}
  params:
    - name: search
      value: "{{search}}"
      type: query
  auth: inherit

runtime:
  variables:
    - name: search
      value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

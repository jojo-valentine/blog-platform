info:
  name: get-profile
  type: http
  seq: 9

http:
  method: GET
  url: "{{base_url}}/api/auth/profile"
  headers:
    - name: Cookie
      value: accessToken={{jwt_token}}
  body:
    type: json
    data: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

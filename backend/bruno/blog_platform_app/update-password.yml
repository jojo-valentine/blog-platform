info:
  name: update-password
  type: http
  seq: 8

http:
  method: PATCH
  url: "{{base_url}}/api/auth/reset/update/password/"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "password": "password@P123",
        "password@P123":  "password@P123"
      }
  auth:
    type: basic

runtime:
  scripts:
    - type: after-response
      code: |-
        // bru.setVar("token", res.body.token);
        // bru.setVar("userId", res.body.user.id);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: blog_post_delete_image
  type: http
  seq: 18

http:
  method: DELETE
  url: "{{base_url}}/api/blog/:id/image"
  headers:
    - name: Cookie
      value: accessToken={{jwt_token}}
  params:
    - name: id
      value: 69d536cac23a601590eaa355
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

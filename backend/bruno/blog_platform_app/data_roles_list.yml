info:
  name: data_roles_list
  type: http
  seq: 35

http:
  method: GET
  url: "{{base_url}}/api/admin/roles/list"
  headers:
    - name: Cookie
      value: accessToken={{jwt_token}}
  auth: inherit

runtime:
  variables:
    - name: id
      value: 6a1df05b7b97fd63907bd88f
    - name: show
      value: "false"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: chan_new_password
  type: http
  seq: 15

http:
  method: PATCH
  url: "{{base_url}}/api/auth/confirm-change-email?token={{token}}"
  headers:
    - name: Cookie
      value: accessToken={{jwt_token}}
      disabled: true
  params:
    - name: token
      value: "{{token}}"
      type: query
  auth: inherit

runtime:
  variables:
    - name: EmailOtp
      value: "145043"
      disabled: true
    - name: newEmail
      value: jo@gmail.com
      disabled: true

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

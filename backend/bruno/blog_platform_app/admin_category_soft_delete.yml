info:
  name: admin_category_soft_delete
  type: http
  seq: 28

http:
  method: DELETE
  url: "{{base_url}}/api/admin/list/categories/{{id}}/delete"
  headers:
    - name: Cookie
      value: accessToken={{jwt_token}}
  auth: inherit

runtime:
  variables:
    - name: id
      value: 6a1dd94a89920d4f2d67187e

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

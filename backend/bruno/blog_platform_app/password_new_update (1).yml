info:
  name: password_new_update (1)
  type: http
  seq: 11

http:
  method: GET
  url: "{{base_url}}/api/auth/test"
  headers:
    - name: Cookie
      value: accessToken={{jwt_token}}
  body:
    type: json
    data: ""
  auth: inherit

runtime:
  variables:
    - name: oldpassword
      value: password@P123
      disabled: true
    - name: newPassword
      value: pass@P123
      disabled: true
    - name: confirmNewPassword
      value: pass@P123
      disabled: true

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

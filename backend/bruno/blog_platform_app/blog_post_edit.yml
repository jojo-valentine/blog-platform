info:
  name: blog_post_edit
  type: http
  seq: 17

http:
  method: GET
  url: "{{base_url}}/api/blog/:id/edit"
  headers:
    - name: Cookie
      value: accessToken={{jwt_token}}
  params:
    - name: id
      value: 69d536cac23a601590eaa352
      type: path
  auth: inherit

runtime:
  variables:
    - name: id
      value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

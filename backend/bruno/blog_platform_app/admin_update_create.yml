info:
  name: admin_update_create
  type: http
  seq: 27

http:
  method: PATCH
  url: "{{base_url}}/api/admin/list/categories/{{id}}/update"
  headers:
    - name: Cookie
      value: accessToken={{jwt_token}}
  body:
    type: json
    data: |-
      {
        "name" : "{{name}}"
      }
  auth: inherit

runtime:
  variables:
    - name: id
      value: 6a1dd94a89920d4f2d67187e
    - name: name
      value: โล้น

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: refresh_user
  type: http
  seq: 23

http:
  method: GET
  url: "{{base_url}}/api/auth/me"
  headers:
    - name: Cookie
      value: refreshToken={{jwt_token_refresh}};accessToken={{jwt_token}}
  body:
    type: json
    data: ""
  auth: inherit

runtime:
  variables:
    - name: email
      value: jojolala731@gmail.com
      disabled: true
    - name: password
      value: pass@P123
      disabled: true
    - name: email
      value: alice@example.com
      disabled: true
    - name: password
      value: password@P123
      disabled: true
  scripts:
    - type: after-response
      code: |-
        bru.setVar("jwt_token", res.body.accessToken);
        bru.setVar("userId", res.body.payload.id);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

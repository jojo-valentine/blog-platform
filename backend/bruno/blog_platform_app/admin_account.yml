info:
  name: admin_account
  type: http
  seq: 45

http:
  method: GET
  url: "{{base_url}}/api/admin/user/account"
  headers:
    - name: Cookie
      value: refreshToken={{jwt_token}}
    - name: Content-Type
      value: multipart/form-data
      disabled: true
  body:
    type: multipart-form
    data:
      - name: ""
        type: text
        value: ""
  auth: inherit

runtime:
  variables:
    - name: id
      value: 6a1f2d2f62b69b02b2b2ca60
    - name: password
      value: password@P123
    - name: confirm_password
      value: password@P123

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: admin_destory_category
  type: http
  seq: 29

http:
  method: DELETE
  url: "{{base_url}}/api/admin/list/categories/{{id}}/destroy"
  headers:
    - name: Cookie
      value: accessToken={{jwt_token}}
  auth: inherit

runtime:
  variables:
    - name: id
      value: 6a1dd94a89920d4f2d67187e

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: blog_post_delete
  type: http
  seq: 19

http:
  method: DELETE
  url: "{{base_url}}/api/blog/:id/delete"
  headers:
    - name: Cookie
      value: accessToken={{jwt_token}}
  params:
    - name: id
      value: 69d536cac23a601590eaa352
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

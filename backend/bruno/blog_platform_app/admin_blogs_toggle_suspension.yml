info:
  name: admin_blogs_toggle_suspension
  type: http
  seq: 22

http:
  method: PATCH
  url: "{{base_url}}/api/admin/:id/blogs/suspension"
  headers:
    - name: Cookie
      value: accessToken={{jwt_token}}
  params:
    - name: id
      value: 69d60bf9df4f2c891ab16542
      type: path
  body:
    type: json
    data: |-
      {
        "action" :"{{action}}"
      }
  auth: inherit

runtime:
  variables:
    - name: action
      value: suspend
      disabled: true
    - name: action
      value: unsuspend

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

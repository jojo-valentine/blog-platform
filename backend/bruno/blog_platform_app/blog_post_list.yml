info:
  name: blog_post_list
  type: http
  seq: 13

http:
  method: GET
  url: "{{base_url}}/api/blog/data-list"
  headers:
    - name: Cookie
      value: accessToken={{jwt_token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

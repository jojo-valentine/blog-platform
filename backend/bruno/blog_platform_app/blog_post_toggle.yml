info:
  name: blog_post_toggle
  type: http
  seq: 20

http:
  method: PATCH
  url: "{{base_url}}/api/blog/:id/toggle"
  headers:
    - name: Cookie
      value: accessToken={{jwt_token}}
  params:
    - name: id
      value: 69d536cac23a601590eaa352
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
